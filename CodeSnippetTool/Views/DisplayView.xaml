<UserControl x:Class="CodeSnippetTool.Views.DisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:CodeSnippetTool.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:DisplayViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Gray"
>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="45"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="45"/>
            <!--<ColumnDefinition Width="25"/>-->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition />
            <!--<RowDefinition />-->
        </Grid.RowDefinitions>
        <Grid Grid.Column="1" Background="#0F0F2D">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1">

            </StackPanel>
        </Grid>

        <Grid Grid.Column="1" Grid.Row="1" >
            <ScrollViewer x:Name="Scroll"  Background="red"  >
                <Grid   Background="LightBlue" x:Name="SnippetContainer" DataContext="{Binding SnippetContainer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
              >
                    <Grid.ColumnDefinitions  >
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="150" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <!--<Grid.RowDefinitions  DataContext="{Binding DisplayViewModel}" LoadCommand="{Binding NavigateAddingCommand}">-->
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="100" />
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="100"/>


                    </Grid.RowDefinitions>
                    <TextBlock Text="hot-key"  x:Name="snippetHotKey" Foreground="Green" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center"
                           HorizontalAlignment="Center" FontSize="20px"></TextBlock>
                    <!--<TextBlock Text="descirption" x:Name="snippetDescription" Foreground="Green" Grid.Column="1" Grid.Row="0" VerticalAlignment="Center"
                           HorizontalAlignment="Center" FontSize="20px"></TextBlock>
                    <TextBlock Text="date when used" x:Name="snippetLastUsedDate" Foreground="Green" Grid.Column="2" Grid.Row="0" VerticalAlignment="Center"
                           HorizontalAlignment="Center" FontSize="20px"></TextBlock>-->

                </Grid>
            </ScrollViewer>
        </Grid>

        <!--<StackPanel Grid.Row="1" Grid.Column="1" Background="white">

        </StackPanel>-->

        <Button Content="Button" HorizontalAlignment="Left" Margin="0,95,0,0" Grid.Row="1" VerticalAlignment="Top" Click="Button_Click"/>
        <Button Content="Display page button" Command="{Binding NavigateAddingCommand}" HorizontalAlignment="Left" Margin="10,292,0,0" Grid.Row="1" VerticalAlignment="Top"/>
        <Button Content="ViewModel" Command="{Binding DisplayCommand}" Grid.Column="2" HorizontalAlignment="Left" Margin="5,160,0,0" Grid.Row="1" VerticalAlignment="Top"/>
        <Button Content="Parameter" Command="{Binding DisplayParamCommand}"  Grid.Column="1" HorizontalAlignment="Left" Margin="487,252,0,0" Grid.Row="1" VerticalAlignment="Top"/>
        
        <!--<Button Content="Add simple snippet" Command="{Binding NavigateAddingCommand}" Margin="587,225,58,170"/>-->
    </Grid>
</UserControl>
